% Trapezoidal rule for single integration

synms x
x_values = input("Enter the values of x as a matrix :");
func = 
n = length(x);
h = x(2)- x(1);

a = x(1);b = x(n);

integration_res = (h/2) * ((y(1)+y(length(y))) + 2*(y(2:n-1)));

fprintf()